.PHONY = all clean oclean fclean re

CC = gcc
SRC = main.c school.c
OBJ = $(SRC:%.c=%.o)
NAME = school
RM = rm -f

all: $(OBJ)
	${CC} $(OBJ) -o ${NAME}
%: $(basename %).c
	${CC} -c -o $@ $<
clean:
	${RM} *~ school
oclean:
	${RM} $(OBJ)
fclean: clean oclean
re: oclean all
